void qparm_colvar(void)
{
  do { _Complex float ylm; float nbond; ylm = 0; nbond = 0; ylm = ylm / nbond; } while (0);
  do { _Complex double ylm; double nbond; ylm = 0; nbond = 0; ylm = ylm / nbond; } while (0);
  do { _Complex long double ylm; long double nbond; ylm = 0; nbond = 0; ylm = ylm / nbond; } while (0);

  do { _Complex char ylm; char nbond; ylm = 0; nbond = 0; ylm = ylm / nbond; } while (0);
  do { _Complex short ylm; short nbond; ylm = 0; nbond = 0; ylm = ylm / nbond; } while (0);
  do { _Complex int ylm; int nbond; ylm = 0; nbond = 0; ylm = ylm / nbond; } while (0);
  do { _Complex long ylm; long nbond; ylm = 0; nbond = 0; ylm = ylm / nbond; } while (0);
  do { _Complex long long ylm; long long nbond; ylm = 0; nbond = 0; ylm = ylm / nbond; } while (0);
}
