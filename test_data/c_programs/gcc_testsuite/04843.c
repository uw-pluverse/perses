


void bar (int a[10][10][10]);
extern int f[][2];
extern int g[];
void
foo (int a[10][10][10], int **b, int x)
{
  int c[10][10][0];
  int d[0];
  char e[12];
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
 bar (a);
}
